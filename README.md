# 🎲 요트 다이스 (YORR)

휴대폰을 흔들거나 버튼을 눌러 주사위를 굴리는 **실시간 멀티플레이 요트 게임** 프로토타입.
방을 만들어 링크/입장코드를 친구에게 공유하면, 여러 명이 **동시에** 같은 게임을 플레이하고
서로의 주사위·점수를 실시간으로 봅니다.

> 팀 본 프로젝트(Spring + WebRTC)로 가기 전, 핵심(센서 굴리기 · 방/입장 · 실시간 동기화 · 요트 룰)을
> 빠르게 검증하는 개인 탐색용 스파이크입니다. 속도 우선이라 실시간은 Socket.IO 로 구성했습니다.

---

## 🎮 미니게임 (develop 부가 콘텐츠)

대기방/로비에서 혼자 또는 폰끼리 즐기는 부가 미니게임 모음. 게임 선택 화면(GameHub)에서 카드로 고른다.
각 게임은 `client/src/games/<게임>/` 아래에 있고, 게임 루프는 `useRef`, UI 노출값만 `useState` 로 관리한다.

| 게임 | 설명 | 입력 |
|------|------|------|
| 🏓 **핑퐁 스매시** | 날아오는 공을 타이밍 맞춰 스매시 | 폰 흔들기 / 키보드 / 온라인 1:1 |
| 🎵 **리듬 탭** | 내려오는 네온 노트를 Perfect·콤보로 | 화면 탭 / 키보드 `D F J K` / 온라인 1:1 |
| ⚡ **반응속도 배틀** | 초록 신호에 최대한 빨리 반응(ms) | 탭·휘두르기 / 키보드 `Space` / 온라인 1:1 |
| 🗡️ **기술스택 슬래셔** | 날아오는 기술 로고를 광선검으로 베기(60초) | 터치·마우스 스와이프 (완전 클라이언트) |

> **기술스택 슬래셔** 는 서버·소켓 없이 도는 순수 클라이언트 게임이다. Canvas 2D 로 오브젝트·블레이드 궤적을
> 그리고, 벤 스택을 분야(FE·BE·Infra·AI)별로 모아 **개발자 유형**을 판정한다.
> 로고는 외부 에셋 없이 전부 캔버스로 직접 그린다. 코드: [client/src/games/slasher/](client/src/games/slasher/)

---

## 📁 폴더 구조

```
YORR/
├─ client/                     # 프론트엔드 (React + Vite + TS + TailwindCSS)
│  └─ src/
│     ├─ App.tsx               # 화면 라우팅 (방 상태에 따라 Home/Lobby/Game/Results)
│     ├─ screens/              # 화면들
│     │  ├─ Home.tsx           #   방 만들기 / 참가
│     │  ├─ Lobby.tsx          #   대기실 (입장코드 + 공유 링크 + 플레이어 목록)
│     │  ├─ GameScreen.tsx     #   게임 (주사위 킵/굴리기 + 점수판 + 상대 현황)
│     │  └─ Results.tsx        #   결과 순위
│     ├─ components/
│     │  ├─ Dice.tsx           # 주사위(점 표시 + 날아오는 굴리기 애니메이션)
│     │  └─ Scorecard.tsx      # 요트 점수판 (예상 점수 미리보기 → 탭해서 확정)
│     ├─ game/yacht.ts         # ★요트 규칙·점수 계산 (순수 로직)
│     ├─ net/                  # 서버 통신 (socket.ts / useRoom.ts / types.ts)
│     ├─ hooks/useMotionDice.ts# 센서(흔들기/던지기) 감지
│     └─ lib/feedback.ts       # 진동/효과음/화면흔들림
├─ server/                     # 실시간 서버 (Node.js + Socket.IO)
│  └─ index.js                 # 방/참가/동시 라운드 진행/브로드캐스트
├─ 로컬플레이.bat             # ★같은 와이파이/핫스팟에서 플레이 (더블클릭)
├─ local-play.ps1             #   (로컬플레이.bat 이 호출하는 스크립트)
├─ 온라인플레이.bat           # ★멀리 있는 친구와 온라인 플레이 (ngrok, 더블클릭)
└─ online-play.ps1            #   (온라인플레이.bat 이 호출하는 스크립트)
```

---

## ✅ 사전 준비물

- **Node.js 18+** (개발 PC는 v24 설치됨)
- 테스트용 스마트폰: **갤럭시(Chrome)** 또는 **아이폰(Safari)**
- 여러 명 테스트하려면 폰이 여러 대 (또는 PC 브라우저 여러 탭)

---

## 🚀 실행하기

### 방법 ① 원클릭 (추천)

- **같은 와이파이/핫스팟**에서 플레이 → **`로컬플레이.bat`** 더블클릭
  - 서버(3001) + 웹(5173, https)이 뜨고, 폰 접속 주소(`https://172.20.10.x:5173/`)가 표시됨.
- **멀리 있는 친구와 온라인** 플레이 → **`온라인플레이.bat`** 더블클릭
  - 빌드 후 서버가 앱+소켓을 서빙하고, ngrok 공개주소(`https://xxxx.ngrok-free.dev`)를 발급 → 그 주소를 공유.
- (`.ps1` 을 직접 우클릭 실행하면 Windows 실행정책 때문에 창이 닫힐 수 있어, 반드시 `.bat` 을 쓰세요.)

### 방법 ② 터미널 2개 직접

```bash
# 터미널 1 — 실시간 서버
cd server
npm install      # 최초 1회
npm run dev      # 포트 3001

# 터미널 2 — 웹(클라이언트)
cd client
npm install      # 최초 1회
npm run dev      # https://localhost:5173
```

> 웹(5173)은 `/socket.io` 요청을 서버(3001)로 **프록시**합니다(`vite.config.ts`).
> 덕분에 폰은 **5173 한 포트만** 열면 실시간 통신까지 됩니다. (CORS/추가 포트 불필요)

---

## 📱 폰에서 테스트 (SSAFY 와이파이 대응)

SSAFY 등 학교/회사 와이파이는 **기기 간 통신을 막아서(AP 격리)** 폰↔PC 접속이 안 됩니다.
그래서 **폰 핫스팟**을 쓰는 게 가장 확실합니다.

1. **폰에서 개인용 핫스팟 켜기**
2. **노트북 Wi-Fi 를 그 핫스팟에 연결**
3. 위 실행 스크립트 실행 → 뜨는 **`https://172.20.10.x:5173/`** 주소를 폰 브라우저에 입력
4. 인증서 경고 → 계속 진행 (아이폰: "세부사항 → 웹사이트 방문")

> 💾 **데이터 걱정 X**: 폰↔노트북 통신은 핫스팟 로컬 링크 안에서만 오가고 셀룰러를 안 탑니다.
> 게임도 노트북이 로컬로 서빙하므로 **플레이 중 데이터 사용은 사실상 0**입니다.
> (걱정되면 Windows Wi-Fi 설정에서 그 핫스팟을 "종량제 연결"로 켜두면 백그라운드 사용도 차단됩니다.)

---

## 🕹️ 게임 방법

1. **닉네임 입력** → **[방 만들기]** (또는 친구가 준 링크/코드로 **[참가하기]**)
2. **대기실**: 입장코드·링크를 친구에게 공유. 다 모이면 방장이 **[게임 시작]**
3. **게임 (동시 플레이)**: 라운드마다 모두가 각자
   - **[🎲 굴리기]** 버튼(또는 폰 흔들기)으로 굴림 — 주사위가 **오른쪽에서 굴러 떨어져** 트레이에 놓임
   - 남기고 싶은 주사위를 **눌러 상단 KEEP 칸으로 올림**(다시 누르면 트레이로 내려옴), 나머지만 다시 굴림 (**최대 3번**)
   - 오른쪽 **점수표에서 내 열의 칸을 눌러** 확정 (칸마다 "지금 넣으면 몇 점"인지 라임색으로 미리 보임)
   - 모두가 확정하면 다음 라운드로. **12라운드** 후 종료
   - 하단에서 **민감도(안정/보통/민감) · 모션 · 진동 · 사운드** 를 켜고 끌 수 있음
4. **결과**: 총점 순위 표시

### 📖 점수 규칙 (한국에서 통용되는 요트다이스 룰)

윗줄:
- **1~6**: 해당 눈의 합 (예: `4`는 4가 나온 개수 × 4)
- **보너스**: 윗줄(1~6) 합이 **63점 이상**이면 **+35점**

아랫줄:
| 족보 | 조건 | 점수 |
|------|------|------|
| 초이스 | 아무 조합 | 주사위 5개 합 |
| 포카드 | 같은 눈 4개 이상 | 주사위 5개 합 |
| 풀하우스 | 3개 + 2개 | 주사위 5개 합 |
| S.스트레이트 | 4개 연속 | 15점 |
| L.스트레이트 | 5개 연속 | 30점 |
| 요트 | 5개 모두 같음 | 50점 |

> 참고: 1938년 원조 Yacht 룰과 한국식(야찌 보너스 포함) 룰에 차이가 있어, 한국 플레이어에게
> 익숙한 방식(스몰/라지 스트레이트 · 윗줄 63점 보너스)을 채택했습니다.
> 규칙은 [game/yacht.ts](client/src/game/yacht.ts) 한 곳에 모여 있어 쉽게 바꿀 수 있습니다.
> 출처: [Yacht (Wikipedia)](https://en.wikipedia.org/wiki/Yacht_(dice_game)), [요트(게임) - 나무위키](https://namu.wiki/w/요트(게임))

---

## 🗺️ 진행 상황

- [x] 센서로 흔들기/던지기 → 주사위 굴리기 (실제 폰 검증 완료)
- [x] 요트 룰 (12족보 + 보너스) · 5개 주사위 · 킵/리롤 3회
- [x] 주사위 날아오는 굴리기 애니메이션
- [x] Socket.IO 서버 · 방 만들기/참가(입장코드) · 대기실
- [x] 동시 라운드 플레이 + 상대 주사위/점수 실시간 표시 · 결과 순위
- [ ] 입장코드 **QR 코드** 표시 (다음 단계)
- [ ] 라운드 타이머(20~25초) · 재접속 처리

---

## 🧩 기술 스택

| 영역 | 선택 | 이유 |
|------|------|------|
| 프론트 | React + Vite + TS + Tailwind v4 | 혼자 빠르게, 모바일 반응형 |
| 로컬 HTTPS | `@vitejs/plugin-basic-ssl` | 센서는 https 필수 |
| 실시간 | Node.js + Socket.IO | 방/동기화를 가장 단순하게 |
| 상태 저장 | 서버 메모리 | 프로토타입이라 DB 생략 (재시작 시 초기화) |
